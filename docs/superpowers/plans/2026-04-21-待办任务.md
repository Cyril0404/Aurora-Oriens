# Oriental Treasure · 待办任务清单

> 基于 Superpowers 框架整理
> 日期：2026-04-21
> 状态：以下任务可直接分配给 Claude Code 执行

---

## ✅ 可立即执行的任务

### Task 1：SEO 技术规范

**目标：** 让网站对搜索引擎友好

**操作步骤（Claude 直接执行）：**

- [ ] 创建 `src/robots.txt`
  ```
  User-agent: *
  Disallow: /src/
  Allow: /
  Sitemap: https://[domain]/sitemap.xml
  ```

- [ ] 创建 `src/sitemap.xml`（包含所有可访问页面URL）

- [ ] 在 `主页设计.html` 和 `products.html` 的 `<head>` 里加：
  - 唯一 `<title>`
  - 唯一 `<meta name="description">`（150字符内）
  - OG tags（og:title / og:description / og:image）
  - `rel="canonical"` 标签

- [ ] 所有 `<img>` 标签添加 `alt` 属性（无则留空字符串）

---

### Task 2：移动端响应式优化

**目标：** 现有页面在 iOS Safari / Android 正常显示

**操作步骤（Claude 直接执行）：**

- [ ] 读取 `主页设计.html` 和 `products.html` 的 CSS
- [ ] 逐一检查：
  - 按钮点击区域 ≥ 44×44px
  - 字体大小 ≥ 14px
  - 表格/图片设置 `max-width: 100%`
  - 横向滚动禁止（`overflow-x: hidden`）
  - 购物车结算在手机上可正常操作
- [ ] 修复发现的问题

---

### Task 3：创建 404 页面

**目标：** 404 错误时显示品牌友好页面

**操作步骤（Claude 直接执行）：**

- [ ] 创建 `src/404.html`
- [ ] 风格与主页一致（象牙白 + Playfair Display）
- [ ] 内容：
  - 标题：「看起来这个页面不在了」
  - 副标题：品牌语
  - 按钮：「返回首页」「查看产品」
- [ ] 设置 HTTP 404 响应（需服务器配置，可后续处理）

---

### Task 4：About 页面

**目标：** 让用户了解品牌故事和匠人背景

**操作步骤（Claude 直接执行）：**

- [ ] 参考 `docs/内容资产.md` 里的「About Us / Brand Story」部分
- [ ] 创建 `src/about.html`
- [ ] 结构：
  - Hero：大图 + 品牌标语
  - 品牌故事（用内容资产里的文字）
  - 匠人介绍（3个工艺的故事：Gold Filigree / Cloisonné Enamel / Jade Micro-Carving）
  - 品牌承诺（Artisan Crafted / Certified Quality / Global Delivery）
  - Newsletter 订阅区域
- [ ] 导航栏加上「Our Story」链接
- [ ] SEO：title + meta + OG tags

---

### Task 5：Contact 页面

**目标：** 用户能联系品牌/提出问题

**操作步骤（Claude 直接执行）：**

- [ ] 创建 `src/contact.html`
- [ ] 结构：
  - 页面标题：「Get in Touch」
  - 联系信息（邮箱 / 社交媒体链接）
  - 联系表单（Name / Email / Message / Submit）
  - FAQ 区域（5-8个常见问题，内容来自内容资产.md）
- [ ] 表单提交行为（假提交 → 显示成功提示，实际可用 Formspree / Mailto）
- [ ] 导航栏加上「Contact」链接
- [ ] SEO：title + meta + OG tags

---

## 🔶 需要先做 SPEC（再执行）

### Task 6：产品详情页
- 需要先出 SPEC（涉及页面设计/交互）
- 当前商品数据在 `products.json`，详情页需要读取

### Task 7：支付方案
- 方案未定（Shopify 排除，待神冢确认方向）
- 可先做「假支付」UI，实际接入 Stripe / PayPal 收款链接

---

## 📋 执行顺序建议

```
1. SEO 规范（Task 1）      → 最快，30分钟
2. 移动端优化（Task 2）     → 1-2小时
3. 404 页面（Task 3）      → 30分钟
4. About 页面（Task 4）    → 2-3小时
5. Contact 页面（Task 5）  → 2-3小时
```

---

## 🎯 执行原则

- 每完成一个任务，Claude 汇报给丞相审核
- 丞相核验后交付神冢确认
- 所有页面遵循设计规范（色彩/字体见 `docs/superpowers/specs/2026-04-21-技术规范.md`）
- 审美拿不准的地方，引用 `TASTE.md` 原则，仍有疑问则停下来问神冢

---

*本文件由丞相维护，按 Superpowers 框架编写。Claude 执行前必读。*